# Development dependencies for local testing
# Run this on Windows host to test API without Docker

aiohttp>=3.8.0
requests>=2.28.0
Pillow>=9.0.0

# Optional: for better JSON handling
ujson>=5.0.0

# Optional: for better logging
colorlog>=6.7.0
